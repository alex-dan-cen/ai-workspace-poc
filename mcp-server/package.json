{
  "name": "mcp-multi-agent-framework",
  "version": "0.1.0",
  "description": "Headless Multi-Agent MCP orchestrator (StdIO) connecting code editors to GitHub/Jira/SonarQube MCP servers with steering, sandboxing, distillation, parallelization, telemetry and behavior tracking.",
  "type": "module",
  "bin": {
    "mcp-multi-agent": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p .",
    "start": "node dist/index.js",
    "dev": "tsc -p . --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18"
  }
}